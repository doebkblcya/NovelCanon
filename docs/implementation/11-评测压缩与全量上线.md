# 11 评测、压缩与全量上线

## 目标

通过短篇和长篇 Pilot 冻结质量与成本基线，决定是否启用压缩，随后完成全量建库、备份恢复、API 和持续回归。

## 前置条件

- 阶段 01–10 已形成完整、可计量的管线。
- 原文不压缩基线能够稳定运行。
- 验收阈值在查看正式评测结果前写入测试配置。

## P1：短篇整本 Pilot

使用约 30 万字完整小说，对比：

1. 原文 hybrid 基线；
2. 原文结构化管线；
3. 可选压缩结构化管线。

评测至少包含：

- 核心/全部实体合并 F1；
- 事实 precision/recall；
- direct evidence hash 复现率；
- QA 引用章节正确率；
- 因果边 precision；
- 重试和原子激活；
- 每万字 Token、时间和成本。

阈值以《定版方案》§14.2 为准。

## 压缩实验

### 1. 规则预扫描

扫描专名、数字、时间、高频意象和重复短语，生成逐字保留词典，不调用生成模型。

### 2. 受约束改写

每段输出 keep、rewrite、drop、reason 和稳定 ref_seg。题材及输出保留率记录在 compression version 中。

### 3. 后验校验

比较实体、数字、时间表达和豁免词覆盖率；低于阈值的段自动回退原文。

### 4. 决策门

只有压缩管线事实 recall 相对原文线下降不超过 0.02，证据复现和关键 QA 不退化，且成本收益足够明确时，才允许默认启用。不同题材可采用不同策略。

## P2：长篇质量 Pilot

使用连续 200–300 章主样本，并增加开头、中段和结尾窗口。重点验证：

- 跨百章实体合并；
- 后期身份揭示；
- 长程状态和关系演变；
- 因果链断裂率；
- cutoff 与 world time 独立安全性；
- 卷/全书摘要覆盖率；
- 数据库、索引和队列的线性扩展性。

## P3：全量建库

全量前完成：

- 容量与 Token 预测；
- 数据库和原始输入备份；
- checkpoint 恢复演练；
- 索引重建演练；
- 磁盘空间和 WAL 监控；
- 限流及日预算；
- 失败章节人工处理流程。

全量运行采用逐阶段闸门，不应在 Map 未验收时继续执行 canonical/link/reduce。

## P4：查询 API

- API 调用复用 CLI 的 application service。
- 请求必须包含明确 book_id。
- 支持 knowledge cutoff 和 world at chapter。
- 返回事实、证据、章节和运行版本。
- 增加超时、分页、限流、审计日志和稳定错误码。
- 不在 API 层复制时间过滤业务逻辑。

## P5：持续评测

- 冻结黄金集并纳入 CI/定期任务。
- Prompt、profile、ontology、Schema、索引或查询改动均触发对应回归。
- 分别观察质量、延迟、吞吐和 Token。
- 新增事实类型或查询路线前先修改定版方案。
- 定期执行数据库完整性、证据复现和 cutoff 泄露扫描。

## 产物

- P1/P2 评测报告；
- 三路线质量与成本对比；
- 压缩启用/禁用决策记录；
- 全量运行手册；
- 备份、恢复和重建工具；
- 查询 API；
- 持续回归集和监控指标。

## 验证

- P1/P2 达到冻结阈值。
- 全量任务可以从模拟故障中恢复。
- 数据库恢复后通过 foreign key 和证据完整性检查。
- API 与 CLI 对相同查询返回一致事实集。
- cutoff 泄露回归为零。
- Token 账本覆盖全部模型调用。

## 最终退出标准

- 目标长篇完成单一 active 全量 run。
- 所有正式回答可追溯到 supported evidence。
- 备份、恢复、索引重建和失败重跑均经过演练。
- 质量、成本、吞吐和错误率有持续可观察基线。
- 后续变更遵循“先契约、再 migration、再代码、最后重建/回归”的流程。

## 实现记录（阶段 11 交付）

### 模块

- 评测：`eval/`（golden.py 黄金集 / metrics.py 指标计算 / pilot.py 三路线
  Pilot）——QA 引用章节正确率、事实 precision/recall、实体合并 F1、
  证据 hash 复现率、因果边 precision；Pilot 报告 JSON 可序列化；
  fixture 黄金集由 GOLDEN_CHAPTERS（4 章黄金样例）构造并冻结。
- 压缩：`compression/`（prescan 规则预扫描→rewriter 受约束改写
  keep/rewrite/drop/reason/ref_seg→PostValidator 后验校验自动回退→
  DecisionGate 决策门）——决策门四条件（recall 差 ≤0.02 / 证据复现不
  退化 / QA 不退化 / 成本节省 ≥10%）才允许默认启用压缩。
- 备份恢复：`storage/backup.py`——SQLite 在线备份（WAL 安全）+ 恢复 +
  verify_integrity（FK / 证据 span hash 复现 / claims 幂等）。
- 持续回归：`ops/scan.py`——scan_cutoff_leakage（来源章节 ≤ cutoff，
  泄露回归为零）+ scan_integrity；CI 增加黄金集回归 + 完整性扫描步骤。
- 查询 API：`api.py`（FastAPI）——复用 QueryExecutor（不在 API 层复制
  时间过滤逻辑），book_id 必填、cutoff/world_at、分页、进程内限流、
  审计日志、稳定错误码（missing_book/book_not_found/rate_limited/
  invalid_params/internal）、/explain 路由诊断。
- CLI：pilot / compress / backup / restore / scan-leakage / serve。
- 全量运行手册：docs/implementation/11-全量运行手册.md（容量预测、备份
  恢复演练、逐阶段闸门、监控、失败处理、索引重建、最终验收）。

### 关键决策

1. 黄金事实描述与 _predict_facts 提取格式对齐（canonical id 而非表面名），
   保证事实 precision/recall 可比。
2. 压缩确定性路径（无模型）可测；LLM 改写器接口预留（keep/rewrite/drop/
   reason/ref_seg 结构化输出）。
3. 后验校验的段索引与 Compressor 段列表一致（过滤空段后对齐），避免
   索引错位导致全量误回退。
4. Pilot 三路线：structured（QueryExecutor）/ hybrid（RetrievalService
   章节定位）/ compressed（决策门通过后接入）。

### 验证对照

- 指标计算器单元测试 + Pilot 黄金 4 章端到端（事实 recall>0、cutoff
  过滤后 recall 下降）；
- 压缩保留词典/逐段决策/后验回退/决策门四条件；
- 备份→删库→恢复→FK 与证据完整性通过；外键破坏可检出；
- API 与 QueryExecutor 同查询一致事实集（章节集合相同）、限流/错误码/
  cutoff/world_at/分页；
- cutoff 泄露扫描真实库 9 检查零泄露。

### 验收修复记录（阶段 11 复审 P0×6 + P1×1 闭环）

1. **[P0] CI/API 依赖**：`uv sync` 增加 `--extra api`；api extra 固定
   `httpx2`（starlette>=1.6 的 TestClient 弃用 httpx 传输层，httpx2 提供
   无弃用警告、不阻塞的测试客户端）；uv.lock 同步。
2. **[P0] Pilot 证据黄金集**：`GoldenSet.evidence_spans` 冻结 8 条人工
   证据 span（与 make_golden_drafts 直接证据原文逐字一致）；Pilot 正式
   断言证据复现率 = 1.0、逐类型（state/relation/event）事实 recall = 1.0、
   因果 precision = 1.0（人工标注因果边以 supported 验证落库）、核心/全部
   实体合并 F1 分层报告；token/成本/吞吐写入 usage 报告（P1 每万字
   Token/时间/成本的框架就绪，正式阈值依赖真实 LLM 语料）。
3. **[P0] state 事实主体**：`_active_claims` 的 state payload 补齐
   `subject_entity_id`（此前生成 `? 的 cultivation_realm`，黄金 state
   事实永不命中）。
4. **[P0] 备份完整性**：`IntegrityReport.ok` 纳入「已检查证据全部复现」
   （证据 hash 全损坏 → ok=False）；`restore_database` 校验**恢复出的目标
   库**（独立引擎），不再校验原 engine；损坏 hash 反向测试 + 恢复目标库
   测试。
5. **[P0] QA 指标分母**：`qa_chapter_accuracy` 分母恒为全部黄金问题，
   缺失回答记未命中（answered=False）——少返回答案不得抬高准确率。
6. **[P0] 压缩后验兜底**：逐段校验实体/数字/时间覆盖率（任一低于阈值
   回退）；`drop` 段若仍含专名/数字/时间（LLM 错删）强制回退原文并计
   `restored_drop_segments`；整章 `passed` 要求综合 + 数字 + 时间覆盖率
   达标且无被回退的 drop 段。
7. **[P1] API 契约**：请求超时（默认 30s → 稳定错误码 408 timeout，
   executor_factory 可注入测试）；响应返回 active run / index / profile
   版本；pydantic 校验错误统一为 `invalid_params` 形状；`missing_book`
   错误码实际使用（缺 book_id → 400）。

### 真实库复验（修复后）

- 完整性（全量证据校验）：FK 违规 0、证据复现 32/32、claims 重复 0、ok；
- cutoff 泄露扫描（临时副本重建 FakeEmbedder 索引）：12 检查零泄露；
- 备份（1637 章 + 32 claims）→ 恢复 → 完整性通过；
- 压缩实验（第 1–3 章）：保留率 99.15%、后验 3/3、回退段 0、drop 回退 0
  （新后验逻辑无回归）。

### 验收修复记录（阶段 11 复审二轮：P0×4 + P1×1 闭环）

1. **[P0] API 测试阻塞**：弃用 fastapi 0.141/starlette 1.6（TestClient 在
   锁定环境阻塞 /health），api extra 固定成熟组合
   `fastapi>=0.115,<0.116`（starlette 0.46.x + httpx 0.28）——Python
   3.13/3.14 全新环境均验证 /health 即时响应、全量 pytest 正常退出
   （3.14 关键路径 33 测试通过）。
2. **[P0] QA 阈值与路由**：冻结阈值断言 accuracy ≥ 0.95（实测 1.0）；
   修复「小石拜入谁的门下」——「门下」从 org_membership 关键词移到
   relation（拜师是师徒关系），「真名」进 entity_state 关键词（不再
   落 fallback），结构化 QA 5/5 命中。
3. **[P0] 正式 Pilot 入口**：GoldenSet 支持 JSON 序列化/加载
   （schema `golden-v1`，含结构化 claims + 书内容 hash）；
   CLI `pilot --golden file` 加载并校验 book_id / 章节 ordinal 范围 /
   书内容 hash，错配直接拒绝（不再对任意 book 误用 fixture 黄金集）。
4. **[P0] 压缩路线假通过**：
   - 决策门新增**后验硬前置**：所有章节 passed_validation 为 False 时
     直接拒绝启用（reasons.validation），不再作为旁路诊断字段；
   - 压缩路线改为**真实压缩 → 重抽取 → 查询**：黄金 claims 证据 span 在
     压缩文本上重新定位后，在**独立临时库**按 materialize 契约真实落库
     （证据 hash 校验 / alias / observation / 激活 / 索引），再对压缩书
     跑真实 QueryExecutor QA——不再用「证据 span 存在」代理、消除
     all([]) 真空通过；
   - Prescanner 高频词过滤不含汉字的 gram（「。\n」噪声误报覆盖率）。
5. **[P1] API 生产后端**：不再硬编码 FakeEmbedder(8)，按 active index
   的 embedding_profile_id 解析维度创建运行时后端（profile 与索引一致
   才可通过检索层校验）；测试经 executor_factory 注入 fake；新增
   非默认维度（fake-embed-v16）索引下 hybrid 查询回归测试。

### 验收修复记录（阶段 11 复审三轮：P0×3 + P1×2 闭环）

1. **[P0] API 测试卡死根因**：三组 FastAPI/Starlette 组合（0.141+1.6 /
   httpx2 / 0.115+0.46）在锁定环境均阻塞于 **anyio blocking portal**——
   根因是 TestClient 的 portal 机制而非版本。彻底绕开：测试改用
   `SyncASGIClient`（`asyncio.run` 新事件循环 + httpx.ASGITransport，
   不经 portal），**3.13 全量 318 通过、3.14 全量 316+2skip 通过，均
   正常退出**。
2. **[P0] 压缩重抽取注入黄金答案**：诚实标注 `extraction_mode`
   （`golden-replay`：黄金 claims 在压缩文本上的确定性重放，与基线路
   线 golden draft 落库同一 materialize 链路；`llm-map`：正式 P1/P2
   经 `claim_extractor` 注入 LLM Map 抽取器测真实抽取 recall）。
3. **[P0] 丢失证据不进复现率分母**：事实 recall 与证据复现率的分母
   **固定为黄金标准全集**（golden.claims）——抽取器漏抽/压缩删除的
   证据计为未复现，决策门 evidence 条件据此拒绝；新增漏抽场景测试
   （recall<1、复现率<1、evidence=False）。
4. **[P1] 生产 embedding profile**：新增 `retrieval/factory.py`——
   `create_backend(profile_id)` 可插拔工厂（内置 fake-embed-v<N>，
   真实 adapter 经 `register_backend` 注册）；未注册 profile 抛
   `UnknownEmbeddingProfileError`（API 按服务端配置错误返回 500，
   不再 400）。
5. **[P1] 正式黄金集可绕过内容 hash**：`--golden` 正式模式强制
   `book_content_hash` 非空（缺失直接拒绝）；校验扩展为 claims 章节
   范围 + facts/claims 证据 span 逐字存在于对应章节文本。

### 验收修复记录（阶段 11 复审四轮：P0×3 + P1×3 闭环）

1. **[P0] API 测试仍卡死——根因是同步端点经 anyio.to_thread**：第三轮
   的 SyncASGIClient 绕开了 TestClient 的 blocking portal，但 FastAPI
   对同步 `def` 端点仍经 `anyio.to_thread` 执行（锁定环境卡死）。根治：
   **全部端点改为 `async def`**（/health、POST /query、GET /query、
   /explain、RequestValidationError handler 一并 async），由事件循环
   直接 await，完全不经过 anyio 线程机制；阻塞型 executor.ask 用
   stdlib ThreadPoolExecutor + `asyncio.wrap_future` 等待，超时经
   `asyncio.wait_for`（concurrent.futures 线程，非 anyio）。3.13 全量
   323 通过、正常退出（后续轮次新增回归测试，当前全量见文末最新记录）。
2. **[P0] 正式 CLI 压缩 Pilot 仍用黄金重放**：新增
   `eval/extractor.py`——`MapClaimExtractor`（复用阶段 06 Map 流水线：
   GenerationClient + 版本化 prompt + Draft 校验）对压缩文本逐章真实
   抽取，产出 GoldenClaimSpec + 累计 Usage；黄金实体合并对仅用于
   surface→canonical 消歧，不注入黄金 claim 内容。CLI `pilot` 正式路径
   （`--golden --compressed`）自动构造生产抽取器（`build_map_extractor`
   按 NOVELCANON_LLM_* 配置）；未配置 LLM 时报错退出，**不会静默回退
   golden-replay**；`--extraction-mode` 支持显式 golden-replay
   （fixture oracle 辅助验证）。
3. **[P0] 黄金重放可进入启用决策**：决策门（gate-v2）新增
   `extraction_mode` 硬前置——`llm-map` / `production-map` 为生产模式，
   `golden-replay`/未标注直接拒绝启用（不再评估四条件），并有单元测试
   （golden-replay 即使四条件全满足也 enable=False）。
4. **[P1] 生产 embedding 只有注册接口**：新增
   `retrieval/adapters.py`——`OpenAICompatEmbedder`（OpenAI 兼容
   /embeddings HTTP 适配器，l2 归一化 + 维数校验）；`AppSettings` 增加
   `NOVELCANON_EMBEDDING_*` 配置；`retrieval.factory` 增加
   `register_configured_backends`（应用启动时从配置注册真实 adapter，
   create_app 幂等调用）+ `unregister_backend`；本地 HTTP 端点集成测试
   覆盖「真实 adapter 建索引 + API hybrid 查询」全链路。
5. **[P1] 后端配置错误误报 400**：`UnknownEmbeddingProfileError` 基类
   从 ValueError 改为 **RuntimeError**——不会被 API 的 ValueError 分支
   误判为 400；API 显式映射为稳定 **500 backend_not_configured**
   （记录 profile_id，日志 embedding_profile_unconfigured）；新增测试
   （未注册 profile 的 active index → 500 backend_not_configured）。
6. **[P1] usage 非每万字全链路账本**：新增 `eval/usage.py`——
   `StageUsage` + `pipeline_usage_ledger`：rewrite / map / 消歧 /
   evidence_verify / link / reduce 各阶段 input 字符量、调用数、token、
   耗时、名义成本 + **每万字归一化**（tokens/ms/cost ÷ 万字）；压缩路线
   逐阶段计时（rewrite=压缩、map=抽取 LLM usage、消歧/证据验证/链接=
   reingest 各阶段、reduce=未运行如实标注）；structured/hybrid 的 usage
   亦补 per_wan（按查询输入字符量归一）。

### 验收修复记录（阶段 11 复审五轮：P0×2 + P1×1 闭环）

1. **[P0] 正常 /query 请求错误超时（30s 后 408）**：上一轮同步端点 async
   化已绕开 anyio，但 ThreadPoolExecutor + `asyncio.wrap_future` 的完成
   通知在锁定环境仍不可靠——正常查询提交到线程池后 30 秒未完成、触发
   408。根治：**移除线程池，同步 executor（默认 QueryExecutor）在事件
   循环内直接执行**——不提交线程、不依赖任何线程调度/完成通知，正常
   查询必定及时完成；代价是请求期间占用事件循环（低并发 API 可接受，
   uvicorn 多 worker 横向扩展）。超时保护（408）保留给**异步 executor**
   （executor_factory 注入，`inspect.iscoroutinefunction` 检测后经
   `asyncio.wait_for` 可取消）。新增回归测试
   `test_query_normal_query_does_not_timeout`（request_timeout=1s 下正常
   查询必须 200）；`test_query_timeout` 改用异步 SlowExecutor 验证 408。
2. **[P0] 压缩路线与基线路线证据分母不一致**：基线用
   `golden.evidence_spans`（GoldenSet 独立冻结字段），压缩路线误用
   `golden.claims[*].evidence_span`——人工冻结的额外证据只出现在
   evidence_spans 时会被漏掉，缩小分母、虚高复现率、可能错误放行决策
   门。统一：压缩路线（run_compressed_route 与 _reingest_compressed_book）
   的证据复现分母一律用 **golden.evidence_spans 去重**（与基线同一
   canonical 集合）；新增回归测试
   `test_compressed_route_evidence_denominator_matches_baseline`
   （evidence_spans 含不在 claims 中的额外证据 → 两路线 golden_count
   一致、复现率如实 <1.0）。
3. **[P1] 全链路每万字分母重复累计**：`pipeline_usage_ledger` 把各阶段
   input_chars 相加当分母——同一万字语料过 5 个阶段变成「五万字」，
   系统性低估全链路每万字 Token/耗时/成本。改为**显式 `corpus_chars`
   参数作为 totals/per_wan 分母**（压缩路线传原始语料字数），阶段行保留
   各自 input_chars（阶段级每万字各用自身输入量）；测试断言
   totals.input_chars == 原始语料字数。

### 验收修复记录（阶段 11 复审六轮：P0×1 + P1×1 闭环）

1. **[P0] 生产同步查询失去超时保护并阻塞事件循环**：上一轮把同步
   QueryExecutor 改为事件循环内直跑，request_timeout 对生产查询失效、
   慢检索/embedding 卡住时阻塞 /health 与所有并发请求。恢复**可控后台
   执行**：同步 executor 提交到 ThreadPoolExecutor，等待改用**轮询
   `future.done()`**（concurrent.futures 线程安全状态——不依赖
   asyncio.wrap_future 的 call_soon_threadsafe 完成通知，该通知在锁定
   环境不可靠、曾导致正常查询 30s 后 408）；轮询间隙 `asyncio.sleep`
   让出事件循环（/health 与其他请求可响应），deadline 到期返回稳定
   408 timeout；异步 executor 仍走 asyncio.wait_for。新增回归测试：
   `test_query_sync_slow_executor_times_out`（**同步**慢 executor → 408，
   覆盖真实生产路径）、`test_health_responsive_during_slow_query`
   （慢查询期间 /health 即时 200）、`test_query_normal_query_does_not_
   timeout`（收紧超时下正常查询 200）。
2. **[P1] canonical evidence_spans 缺正式校验 + 空集除零**：
   `validate_golden_against_book` 扩展——正式模式（require_content_hash=
   True）下 golden.evidence_spans **非空**（空集复现率无法定义）、逐项
   验证 span 逐字存在于原书文本、拒绝空字符串项（防拼写错误/不存在的
   span 直接进复现率分母）；`_reingest_compressed_book` 复现率计算改为
   复用 `evidence_hash_reproduction`（空集 → 0.0，不再除零）。新增回归
   测试：`test_golden_validation_checks_canonical_evidence_spans`（空集/
   不存在 span/空字符串均拒绝）、`test_compressed_route_empty_evidence_
   spans_no_zerodivision`（空集压缩重入成功不抛 ZeroDivisionError）。
3. 当前全量：**3.13 329 passed 正常退出**（三轮 318 → 四轮 323 → 五轮
   325 → 六轮 329）；3.14 关键路径复验通过；ruff/mypy/git diff --check
   全绿。

### 验收修复记录（阶段 11 复审七轮：P1×2 闭环）

1. **[P1] 超时任务占满线程池、池不随应用关闭**：`future.cancel()` 无法
   停止已运行的线程——超时任务仍占用 worker，连续卡住查询可耗尽 4 个
   worker，后续正常请求排队继续 408。修复：
   - **有界准入**：正在执行的同步查询达到池容量（4）时，新请求立即返回
     稳定 **503 overloaded**（不排队）；
   - **done callback 释放准入计数**：`future.add_done_callback` 在任务
     **在线程中实际完成**时递减计数（GIL 保护、不经过事件循环调度）——
     即使请求早已因超时返回 408，后台线程结束也会释放槽位，池自动恢复；
   - **lifespan 清理**：FastAPI lifespan 退出时 `_pool.shutdown(wait=False,
     cancel_futures=True)` + 统一 close 缓存 backend；`app.state.executor_pool`
     暴露供测试/运维检查。
   回归测试：`test_query_pool_saturation_returns_overloaded_and_recovers`
   （4 个慢查询占满 → 第 5 个 503 → 线程结束后正常查询 200 恢复）、
   `test_api_lifespan_shuts_down_executor_pool`（lifespan 关闭后池拒绝新
   任务）。注意：lifespan 的 __aenter__/__aexit__ 必须在**同一事件循环**
   （asyncio.run 结束时 shutdown_asyncgens 会提前关闭挂起的 generator，
   导致 finally 误执行——测试已按此修正）。
2. **[P1] Embedding HTTP 客户端泄漏**：`OpenAICompatEmbedder` 未注入
   client 时创建拥有型 httpx.Client 但无 close；API 每次查询重新
   create_backend → 连接池持续新建不释放。修复：
   - adapter 实现 **close()/上下文管理器**（仅关闭自有 client，外部注入
     的由外部管理）；
   - API `_runtime_backend` 按 profile **应用级缓存** backend（不随每次
     查询重建，httpx 连接池复用）；lifespan 关闭时统一 close。
   回归测试：`test_openai_embedder_close_lifecycle`（自有 client close
   释放、注入 client 不被关）、`test_api_production_embedding_adapter_
   integration` 扩展连接复用断言（HTTP/1.1 keep-alive 下索引构建与两次
   查询总连接数=2，若每次重建 client 会增长到 3）。
3. 当前全量：**3.13 332 passed 正常退出**（三轮 318 → 四轮 323 → 五轮
   325 → 六轮 329 → 七轮 332）；3.14 关键路径 44 passed；ruff/mypy/git
   diff --check 全绿。
